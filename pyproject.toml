[project]
name = "ai-config-manager"
version = "0.1.0"
description = "Local profile-first AI config manager MVP"
requires-python = ">=3.14"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
